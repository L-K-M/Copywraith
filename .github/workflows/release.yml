name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write # create the GitHub Release and upload assets
  packages: write # push the server image to GHCR

env:
  CARGO_TERM_COLOR: always

jobs:
  ci:
    name: Validate release source
    uses: ./.github/workflows/ci.yml

  validate-version:
    name: Validate release version
    needs: ci
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7

      - name: Match tag to manifests
        run: node scripts/check-release-version.mjs "${GITHUB_REF_NAME#v}"

  # ---------------------------------------------------------------------------
  # Create a draft release that every build job uploads its artifacts into.
  # ---------------------------------------------------------------------------
  create-release:
    name: Create draft release
    needs: validate-version
    runs-on: ubuntu-22.04
    outputs:
      release_id: ${{ steps.create.outputs.result }}
    steps:
      - uses: actions/checkout@v7

      - name: Create draft release
        id: create
        uses: actions/github-script@v9
        with:
          script: |
            const tag = context.ref.replace('refs/tags/', '');
            const { data } = await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: tag,
              name: `Copywraith ${tag}`,
              draft: true,
              prerelease: tag.includes('-'),
              generate_release_notes: true,
            });
            return data.id;

  # ---------------------------------------------------------------------------
  # Desktop bundles: macOS (Apple Silicon + Intel), Linux, and Windows.
  # ---------------------------------------------------------------------------
  build-desktop:
    name: Desktop (${{ matrix.platform }} ${{ matrix.args }})
    needs: create-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: --target aarch64-apple-darwin
            rust-targets: aarch64-apple-darwin
          - platform: macos-latest
            args: --target x86_64-apple-darwin
            rust-targets: x86_64-apple-darwin
          - platform: ubuntu-22.04
            args: ''
            rust-targets: ''
          - platform: windows-latest
            args: ''
            rust-targets: ''
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v7

      - name: Install Tauri system dependencies (Linux)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.120.0
        with:
          targets: ${{ matrix.rust-targets }}

      - name: Cache cargo build
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.platform }}-${{ matrix.args }}

      - name: Install frontend dependencies
        run: npm ci

      - name: Build and upload Tauri bundles
        uses: tauri-apps/tauri-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # macOS bundles ship UNSIGNED on purpose. tauri-action code-signs
          # whenever APPLE_CERTIFICATE is set, and a certificate it cannot
          # import fails the whole build — which blocks publish-release. Read
          # CICD.md, "Signing/notarization caveats", before adding any APPLE_*
          # entry here.
          #
          # Optional Tauri updater signing key (unrelated to Apple signing).
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          releaseId: ${{ needs.create-release.outputs.release_id }}
          args: ${{ matrix.args }}

      # Guards the binary name the Linux shortcut docs depend on.
      - name: Verify the Linux bundle contents
        if: matrix.platform == 'ubuntu-22.04'
        run: ./scripts/check-linux-bundle.sh

      - name: Install and exercise the release deb
        if: matrix.platform == 'ubuntu-22.04'
        env:
          COPYWRAITH_SMOKE_ARTIFACTS: linux-smoke-results
        run: |
          sudo apt-get install -y ./target/release/bundle/deb/*.deb \
            xvfb xauth xclip xdotool openbox dbus-x11 scrot
          python3 scripts/smoke-linux-client.py

      - name: Upload Linux smoke diagnostics
        if: always() && matrix.platform == 'ubuntu-22.04'
        uses: actions/upload-artifact@v4
        with:
          name: release-linux-smoke
          path: linux-smoke-results
          retention-days: 7

  # ---------------------------------------------------------------------------
  # Android APK. Signed when an upload keystore is configured via secrets,
  # otherwise the unsigned release APK is attached.
  # ---------------------------------------------------------------------------
  build-android:
    name: Android APK
    needs: create-release
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - name: Set up JDK
        uses: actions/setup-java@v5.6.0
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android NDK
        run: bash scripts/install-android-ndk.sh

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.120.0
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - name: Cache cargo build
        uses: Swatinem/rust-cache@v2
        with:
          key: android

      - name: Install frontend dependencies
        run: npm ci

      - name: Initialize Android project
        run: npm run tauri -- android init --ci

      - name: Build Android APK
        run: npm run tauri -- android build --ci --apk --target aarch64 armv7 i686 x86_64

      - name: Sign APK
        id: sign
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          APK_DIR="src-tauri/gen/android/app/build/outputs/apk/universal/release"
          UNSIGNED=$(ls "$APK_DIR"/*.apk | head -n1)
          VERSION="${GITHUB_REF_NAME}"

          if [ -z "${ANDROID_KEYSTORE_BASE64:-}" ]; then
            OUT="copywraith-${VERSION}-android-universal-unsigned.apk"
            cp "$UNSIGNED" "$OUT"
            echo "::warning::No ANDROID_KEYSTORE_BASE64 secret set; attaching the unsigned release APK."
            echo "apk=$OUT" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          BUILD_TOOLS=$(ls -d "$ANDROID_HOME"/build-tools/* | sort -V | tail -n1)
          echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > keystore.jks
          ALIGNED="aligned.apk"
          OUT="copywraith-${VERSION}-android-universal.apk"
          "$BUILD_TOOLS/zipalign" -p -f 4 "$UNSIGNED" "$ALIGNED"
          "$BUILD_TOOLS/apksigner" sign \
            --ks keystore.jks \
            --ks-pass "pass:$ANDROID_KEYSTORE_PASSWORD" \
            --ks-key-alias "$ANDROID_KEY_ALIAS" \
            --key-pass "pass:$ANDROID_KEY_PASSWORD" \
            --out "$OUT" "$ALIGNED"
          rm -f keystore.jks "$ALIGNED"
          echo "apk=$OUT" >> "$GITHUB_OUTPUT"

      - name: Upload APK to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${GITHUB_REF_NAME}" "${{ steps.sign.outputs.apk }}" --clobber

  # ---------------------------------------------------------------------------
  # Server Docker image -> GitHub Container Registry.
  # ---------------------------------------------------------------------------
  build-docker:
    name: Server Docker image
    needs: create-release
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Derive image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository_owner }}/copywraith-server
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ !contains(github.ref_name, '-') }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: server/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ---------------------------------------------------------------------------
  # Publish the release once every build job has finished successfully.
  # ---------------------------------------------------------------------------
  publish-release:
    name: Publish release
    needs: [create-release, build-desktop, build-android, build-docker]
    runs-on: ubuntu-22.04
    steps:
      - name: Mark release as published
        uses: actions/github-script@v9
        with:
          script: |
            await github.rest.repos.updateRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id: ${{ needs.create-release.outputs.release_id }},
              draft: false,
            });
